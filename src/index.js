import React from "react";
import ReactDOM from "react-dom";

import Predemo from "./predemo";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Predemo />
  </React.StrictMode>,
  rootElement
);
